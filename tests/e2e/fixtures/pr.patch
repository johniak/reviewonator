diff --git a/src/payments/retry.ts b/src/payments/retry.ts
index 1111111..2222222 100644
--- a/src/payments/retry.ts
+++ b/src/payments/retry.ts
@@ -4,6 +4,11 @@ export async function charge(payment: Payment) {
   try {
     return await provider.charge(payment);
   } catch (error) {
-    throw error;
+    if (error instanceof TimeoutError) {
+      while (true) {
+        return await provider.charge(payment);
+      }
+    }
+    throw error;
   }
 }
diff --git a/src/config.ts b/src/config.ts
index 3333333..4444444 100644
--- a/src/config.ts
+++ b/src/config.ts
@@ -1,3 +1,5 @@
 export const config = {
   timeoutMs: 5000,
+  retryDelayMs: 250,
+  retryEnabled: true,
 };
